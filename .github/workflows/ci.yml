name: CI

on:
  pull_request:
    branches: [ "**" ]
  push:
    branches: [ "main", "master", "dev", "develop" ]
    tags-ignore:
      - 'v*'

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
    steps:
      - uses: actions/checkout@v4

      - name: Checkout SOUL dependency
        uses: actions/checkout@v4
        with:
          repository: benoitpetit/soul
          path: soul

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Initialize Go workspace
        run: |
          echo "use ./soul" > go.work
          echo "use ." >> go.work

      - name: Install dependencies
        run: go mod download

      - name: Run tests
        run: go test -v -race ./...

  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
    strategy:
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v4

      - name: Checkout SOUL dependency
        uses: actions/checkout@v4
        with:
          repository: benoitpetit/soul
          path: soul

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Initialize Go workspace
        run: |
          echo "use ./soul" > go.work
          echo "use ." >> go.work

      - name: Build binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          EXT=""
          if [ "$GOOS" = "windows" ]; then EXT=".exe"; fi
          go build -ldflags="-s -w" -o mira-${GOOS}-${GOARCH}${EXT} ./cmd/mira

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mira-${{ matrix.goos }}-${{ matrix.goarch }}
          path: mira-${{ matrix.goos }}-${{ matrix.goarch }}*